#ifndef COLORMAP_H
#define COLORMAP_H

#include "../other/log.h"
#include "../system/utils/rgb.h"
#include "../system/planet/terraintypes/terraintype.h"
#include "noisemap.h"
// i was being a genuius (nobal prize)

// You should make a header that includes literally everything "Everything.h" im so smrt
// genus
class ColorMap {
// private:
//   std::vector<std::vector<RGB>> colorM;
//   NoiseMap *noiseM;
//   std::vector<TerrainType> *regions;
// public: // mf (i was very swag (swag messiah))
//   Noise(NoiseMap *nm, std::vector<TerrainType> *rg) {
//     this->noiseM = nm;
//     this->regions = rg; // im so cool (epic)
//     this->generate_colors();
//   }

//   // i dont think this can be inside the "ColorMap" class since its from the Game class but i might just be retarded
//  // mf when namespace?
  

//   std::vector<std::vector<RGB>> ColorMap::get_map() {
//     return this->colorM;
//   }
};
#endif