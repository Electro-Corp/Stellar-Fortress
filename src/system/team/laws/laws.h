#ifndef LAWS_H
#define LAWS_H
#include <vector>
class Laws {
private:
	char* name, description;
  Json::Value LawJson.
  int ;
  std::map<std::string, int> properties;
public:
	Laws(char* name, char* lawTeam);
};
#endif
